#include <stdio.h>
#include <stdint.h>

#define ARR_LAST (size_t)(-1)

typedef struct {
  int32_t* data,
  size_t length,
  size_t capacity
} int32arr_t;

/* TODO
+ set
+ get 
+ insert
+ map
*/

int main(void) {
  return 0;
}
